{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — grants exactly the shell-execute permission needed to spawn the bundled bridge sidecar (E8), nothing broader.",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "podium-studio-bridge",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
